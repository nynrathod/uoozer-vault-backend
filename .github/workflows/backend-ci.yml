name: Backend CI/CD

on:
  push:
    branches: [master]
    paths:
      - "src/**"
      - "tests/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "Dockerfile"
      - "migrations/**"
      - ".github/workflows/backend-ci.yml"
  workflow_dispatch:

env:
  REGION: us-central1
  PROJECT_ID: uoozer-vault-prod
  ARTIFACT_REPO: uoozer-vault-prod
  SERVICE: uoozer-vault-backend
  FRONTEND_URL: https://uoozer-vault.vercel.app
  R2_ACCOUNT_ID: db9547fc31e114d3843c33312e69c9f2

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: vault_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run tests
        run: cargo test --verbose
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/vault_test

  build-and-deploy:
    name: Build & Deploy
    # needs: test
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP (Workload Identity Federation — no keys)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.GH_ACTIONS_SA }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker auth
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push image (with layer caching)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.ARTIFACT_REPO }}/backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE }} \
            --image "${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.ARTIFACT_REPO }}/backend:latest" \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --service-account "vault-backend@${{ env.PROJECT_ID }}.iam.gserviceaccount.com" \
            --memory 512Mi \
            --cpu 1 \
            --timeout 300 \
            --min-instances 0 \
            --max-instances 1 \
            --concurrency 80 \
            --cpu-boost \
            --set-env-vars "ENVIRONMENT=production,SERVER_HOST=0.0.0.0,SERVER_PORT=8080,CORS_ALLOWED_ORIGINS=${{ env.FRONTEND_URL }},R2_PRESIGN_TTL_SECONDS=3600,R2_ACCOUNT_ID=${{ env.R2_ACCOUNT_ID }},R2_ENDPOINT=https://${{ env.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com" \
            --set-secrets "DATABASE_URL=database-url:latest,JWT_PRIVATE_KEY_PEM=jwt-private-key:latest,PRELOGIN_PEPPER=prelogin-pepper:latest,R2_ACCESS_KEY_ID=r2-access-key-id:latest,R2_SECRET_ACCESS_KEY=r2-secret-access-key:latest,R2_BUCKET=r2-bucket-name:latest"